import 'package:flutter/material.dart';

class TestScreenScreen extends StatelessWidget {
  const TestScreenScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('ScreenTest'),
        ),
      ),
    );
  }
}
